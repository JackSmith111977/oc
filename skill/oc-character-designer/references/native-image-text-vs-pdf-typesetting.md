# Image text versus deterministic typesetting

Use image generation for short text that is physically part of the scene, such as signs, badges or device displays. Use deterministic PDF/vector composition for canonical descriptions, measurements, callout lines, tables, page numbers, revision metadata and translations.

Store clean and annotated outputs separately so text can change without regenerating the character art.
