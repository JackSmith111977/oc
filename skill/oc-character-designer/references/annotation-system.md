# Annotation system

Categories: `CANON`, `BUILD`, `MATERIAL`, `MOTION`, `ACTING`, `VARIANT`, `AVOID`, `PRODUCTION`.

Each annotation stores an ID, category, short title, concise body, anchor, Canon path, priority and status. Root Canon notes point to `root_canon.*`; hair/costume construction points to `strong_reference.*`.
