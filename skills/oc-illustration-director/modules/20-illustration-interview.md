# Illustration Interview

Use the I0–I8 stages in `references/staged-interview.md`. A batch contains one design dimension or up to three tightly dependent questions in one stage.

Interaction transport:

1. Native Ask/App component when actually available;
2. structured numbered text otherwise.

Always accept custom text. Persist the exact question batch before showing it. Normalize the answer batch and commit it transactionally.
